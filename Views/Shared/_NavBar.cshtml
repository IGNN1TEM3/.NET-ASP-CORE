@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="#">W25350012</a>
    <div class="navbar-nav">
        <a class="nav-item nav-link @(currentController == "Home" && currentAction == "Index" ? "active" : "")" asp-controller="Home" asp-action="Index">Лб 1</a>
        <a class="nav-item nav-link @(currentController == "Product" && currentAction == "Index" ? "active" : "")" asp-controller="Product" asp-action="Index">Каталог</a>
        <a class="nav-item nav-link @(currentController == "Admin" ? "active" : "")" asp-area="Admin" asp-action="Index">Администрирование</a>
    </div>
</nav>
